name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Cargo fmt
        run: cargo fmt --check

      - name: Verify OpenAPI schema snapshot is up to date
        shell: bash
        run: |
          set -euo pipefail
          make generate-client

          if ! git diff --exit-code -- openapi/openapi.json; then
            echo "openapi/openapi.json is out of date with production." >&2
            echo "Run 'make generate-client' and commit the updated snapshot." >&2
            exit 1
          fi

      - name: Validate installer script
        run: |
          sh -n install.sh
          shellcheck install.sh

  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Cargo test
        run: cargo test --locked --all-targets
